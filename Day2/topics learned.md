# Why do we need Client–Server Architecture?
 - we need client server architecture because if we keep entire website data in one device other people cant acess it and if the device goes down entire website will go down
 - instead they are stored in powerful servers which run 24*7 so there are no problems like this
 # Why do we need DNS?
    - we cant remember every website's ip address
    - its just like phonebook of website for example google.com
   # Why do we need HTTP?
   - Your browser and server are different computers.
- How will they understand each other?
- They need common rules.HTTP provides standard rules
 # GET- to get data
 # POST- to send data
 # DELETE- to delete data
 # PUT- to update data
 # HTTPS- basically HTTP+secured
 # Need of API
 - App doesn't directly access databases
 - API handles communication safely
 - # How website remembers me?
 - They create token/cookies
